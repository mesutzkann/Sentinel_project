VITE_API_BASE_URL=http://localhost:5080

# The AI service. MCP tool discovery and calls go here, not through the backend: the backend
# does not proxy them, and the agent talks to the same endpoints from Phase 7.
VITE_AI_SERVICE_URL=http://localhost:8000
